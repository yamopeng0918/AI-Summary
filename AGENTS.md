# AGENTS.md

## 適用範圍

本文件適用於整個 AI Digest repository。所有在本專案工作的代理都必須遵守。

## 專案定位

AI Digest 是一套本機執行的內容摘要與分類工具。系統接收公開網址，擷取內容、產生繁體中文摘要、預測既有分類、保存結構化資料，並透過 GitHub Pages 公開展示。

## 需求優先順序

發生衝突時，依下列順序判斷：

1. 使用者在目前對話中的明確指示。
2. `docs/superpowers/specs/2026-08-09-ai-digest-mvp-design.md`。
3. 最新版 `提案企劃書_彭元懋.docx`。
4. `progress.md` 與 `todo.md`。
5. 其他舊版企畫文件。

若文件彼此矛盾，不得自行混合需求；先依上述優先順序處理，仍無法判斷時詢問使用者。

## MVP 範圍

核心 MVP 包含：

- 公開且可直接讀取的一般網頁。
- YouTube 公開影片，包含無可用字幕的影片。
- 無須登入即可讀取的公開社群單篇貼文。
- 繁體中文摘要、3～5 個重點、分類、標籤、AI 編輯觀點及來源資料。
- 可量化評估且優於最大類基準的分類模型。
- GitHub Pages 列表、詳情、搜尋、分類篩選及日期排序。

未經使用者同意，不得將 PDF／論文、圖片 OCR、網站管理後台、帳號系統、常駐後端或其他加分功能加入核心 MVP。

## 開始工作前

執行任何修改前必須：

1. 讀取本文件、相關設計規格、`progress.md` 與 `todo.md`。
2. 檢查 Git 狀態，辨識使用者既有或未提交的變更。
3. 確認本次工作屬於 MVP，且沒有擴張已核准範圍。
4. 找出適用的現有測試、程式結構與專案命令。
5. 若需求會改變架構、公開介面或資料格式，先提出設計並取得使用者核准。

不得覆寫、還原或刪除不屬於本次工作的使用者變更。

## 測試驅動開發

所有功能、錯誤修正與行為變更都必須採用 TDD：

1. 先寫一個描述預期行為的最小測試。
2. 執行測試，確認它因缺少該行為而失敗。
3. 寫入使測試通過的最小實作。
4. 重跑相關測試及完整測試集。
5. 只在測試保持通過時進行重構。

不得先寫正式程式再補測試。每個新增的正式函式都必須有測試。外部網路與付費 API 不得成為日常測試的必要條件；擷取測試使用本地 fixture，外部服務透過明確介面替換。

設定檔、純文件及自動產生的檔案不要求先寫測試，但仍須執行適當驗證。

## 架構約束

- Python CLI 負責來源辨識、擷取、摘要、分類、驗證與保存。
- 來源解析器不得直接呼叫摘要服務或寫入摘要資料。
- 摘要服務不得負責來源擷取、分類或檔案保存。
- 分類器只接收文字並回傳分類，不修改摘要內容。
- 儲存層必須先通過 Schema 驗證，再寫入獨立 JSON。
- Astro 只讀取已驗證且狀態為 `published` 的資料。
- 前端不得持有密鑰或直接呼叫 OpenAI、NotebookLM。
- 不同來源的解析器必須獨立，單一平台失效不得影響其他來源。
- 相同 `canonicalUrl` 預設拒絕重複建立。
- 下架使用 `archived` 狀態，不直接刪除摘要。

## 安全與來源限制

- 不得提交或輸出 OpenAI API 金鑰、GitHub Token、Cookie、NotebookLM 驗證資料、`.env` 內容或其他憑證。
- 真實設定只保存在本機；repository 只提供不含真實值的 `.env.example`。
- 不得將憑證寫入摘要 JSON、測試 fixture、前端資產、建置輸出、日誌或錯誤訊息。
- 不得嘗試繞過登入、付費牆、反爬蟲、存取控制或私人內容限制。
- 來源不可讀時必須回報明確錯誤，不得產生空白、猜測或看似成功的摘要。
- 執行部署前必須掃描追蹤檔案與建置輸出，確認不含敏感資料。

## 驗證與完成條件

宣稱工作完成、修正成功或測試通過前，必須實際執行與該變更相稱的驗證。

最低要求：

- Python 變更：執行相關 `pytest` 測試；完成里程碑時執行完整測試集。
- Astro 變更：執行相關前端測試及正式建置。
- Schema 或摘要資料變更：驗證所有範例與已保存資料。
- 分類模型變更：執行可重現評估，記錄 Accuracy、Macro F1、混淆矩陣及最大類基準。
- 部署變更：先完成本機正式建置及敏感資料掃描。
- 文件變更：檢查連結、路徑、日期及不同文件間的一致性。

若因環境、憑證或外部服務而無法驗證，必須明確說明未驗證項目與原因，不得推測成功。

## 專案進度文件

完成每個可交付工作單元後，同步更新：

- `progress.md`：階段狀態、完成事項、風險、阻礙、重要決策及下一步。
- `todo.md`：只勾選已實際完成且通過驗證的項目。
- 適用的設計規格或操作文件。

不得為了符合原時程而將未完成工作標記為完成。若實作範圍與進度文件不一致，以實際可驗證成果修正文件。

## Git 與部署

- 開始修改前先檢查 `git status`。
- 保留使用者既有變更，不得使用 `git reset --hard`、強制 checkout 或其他破壞性操作。
- 提交應小而明確，每個提交只包含一個可獨立說明及驗證的工作單元。
- 不得將無關檔案加入提交。
- 提交前執行相關測試與 `git diff --check`。
- 可以在本機建立提交，但未經使用者明確要求，不得 push、建立 Pull Request、修改遠端 repository 或部署 GitHub Pages。
- 部署失敗時保留本機資料與可重試狀態，不得以刪除資料作為修復方式。

## 實作原則

- 優先完成目前里程碑所需的最小可用行為，避免預先建立尚未需要的抽象與功能。
- 使用清楚、單一責任且可獨立測試的模組。
- 錯誤必須使用設計規格定義的階段、代碼、訊息與 `retryable` 結構。
- 時間使用含時區的 ISO 8601 格式，預設時區為 `Asia/Taipei`。
- 技術選擇或資料格式若需要偏離已核准規格，必須先說明原因並取得使用者核准。
